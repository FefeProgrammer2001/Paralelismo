# Calculo de Matriz em C++
### Testando as diferentes formas de multiplicar a matriz, no que diz respeito ao acesso a memória, com a ordem dos laços ijk e ikj
### A ideia é a otimização do uso da Cache da CPU
### Para compilar o código:
    make
### Limpar os binários:
    make clear
